{% extends 'base.html.twig' %}

{% block body %}
    {% for message in app.flashes('erfolg') %}
        <div class="alert alert-success">
        <h3> {{ message }} </h3>
    {% endfor %}
    <br>
    <div class="container">
        <h2>Gerichte bearbeiten</h2>
        <br>
        <table class="table table-stripped">
            <thead>
                <tr>
                    <td>Bestell Nr.</td>
                    <td>Name</td>
                    <td>Beschreibung</td>
                    <td>Preis</td>
                    <td>Löschen</td>
                    <td><a href="{{ path ('app_gerichtapp_anlegen')}}"> Anlegen </a></td>
                </tr>
            </thead>
            <tbody>
                {% for gericht in gerichte %}
                    <tr>
                        <td>{{gericht.id}}</td>
                        <td><a href="{{ path ('app_gerichtapp_anzeigen',{id: gericht.id}) }}">{{gericht.name}} </a></td>
                        <td>{{gericht.Beschreibung}}</td>
                        <td>{{gericht.preis}}</td>
                        {# <td><a href="{{ path ('app_gerichtapp_entfernen',{id: gericht.id}) }}"> x </a></td> #}
                        <td></td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
