{% extends 'base.html.twig' %}

{% block title %}Liste{% endblock %}

{% block body %}
     <br>
    <div class="container">
    <h2>Liste</h2>
    <br>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ liste.name }}</td>
            </tr>
            <tr>
                <th>Stadt</th>
                <td>{{ liste.stadt }}</td>
            </tr>
            <tr>
                <th>Telefon</th>
                <td>{{ liste.telefon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liste_index') }}">zurück</a>

    <a href="{{ path('app_liste_edit', {'id': liste.id}) }}">bearbeiten</a>

    {{ include('liste/_delete_form.html.twig') }}
{% endblock %}
